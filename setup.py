"""Minimal setup.py for backwards compatibility.

All configuration is now in pyproject.toml.
This file exists for compatibility with older pip versions and editable installs.
"""

from setuptools import setup

setup()
